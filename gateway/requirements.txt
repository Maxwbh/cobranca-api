# Requer Python >= 3.12 (exigência da engine pycobranca)
fastapi>=0.111
uvicorn[standard]>=0.51.0
httpx>=0.27
pydantic>=2.13.4
cryptography>=42.0
psycopg[binary]>=3.1
# Engine offline (100% Python) — PyPI, não git: build reprodutível.
#
# Piso na 1.1.0 porque é dela para cima que existe o que o gateway consome:
# `emite_boleto`, `tema_de_api`, os cinco totalizadores FEBRABAN,
# `Retorno.ler(bytes)` e a hierarquia de erros fechada. Com a 1.0.x a API
# prometeria no schema o que a engine instalada não faz.
pycobranca>=1.1.0,<2
pypdf>=4.0        # merge de boletos em PDF único (/api/boleto/multi)
pyyaml>=6.0.3       # spec OpenAPI do offline
python-multipart>=0.0.9  # uploads multipart (/api/remessa, /api/retorno, /api/ofx)
